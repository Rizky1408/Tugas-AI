<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aldi.aplikasiprojectutsaldi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_aldi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".selimutkubus"></activity>
        <activity android:name=".volumekubus" />
        <activity android:name=".selimutkerucut" />
        <activity android:name=".volumekerucut" />
        <activity android:name=".selimutbalok" />
        <activity android:name=".volumebalok" />
        <activity android:name=".balok" />
        <activity android:name=".kerucut" />
        <activity android:name=".kubus" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>